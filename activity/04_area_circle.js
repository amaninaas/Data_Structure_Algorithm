// write a function to calculate area of circle where radius is input and formula is 
// A = pi * square of radius

function area(radius){
    return Math.PI * Math.sqrt(radius)
}

console.log(area(20));