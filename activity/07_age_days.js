// write a function to convert age (in years) to the days. Assume that there are 365 days in 1 years

function days(age){
    return age * 365;
}

console.log("The age ge in days are", days(32));